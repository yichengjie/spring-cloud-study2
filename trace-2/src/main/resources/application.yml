spring:
  application:
    name: trace-2

server:
  port: 2006
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/
 
logging:
  level:
    org.springframework.web.servlet.DispatcherServlet: DEBUG